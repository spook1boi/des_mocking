<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Products</title>
</head>

<body>

<div class="pagination">
    <p>Página: {{page}}</p>
    <p>Total Páginas: {{totalPages}}</p>
    <a href="/?page={{page }}">Página Anterior</a>
    <a href="/?page={{page }}">Página Siguiente</a>
</div>

<div class="infoProd">
    <h1>Product list</h1>
    <div class="detalleProd">
        <div class="product-container">
            {{#each products}}
            <div class="product">
                <h2>{{this.title}}</h2>
                <h3>{{this.description}}</h3>
                <p>Precio: ${{this.price}}</p>
                <p>Categoria: {{this.category}}</p>
                <a href="/api/products/{{this._id}}">Ver Detalle</a>
            </div>
            {{/each}}
        </div>
    </div>
</div>

</body>
</html>